#ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=n$
#update_config=1

network={
    ssid="{{ essid }}"
    psk="{{ psk }}"
}
# wpa_supplicant -dd -i wlan0 -c /etc/wpa_supplicant/wpa_supplicant.conf -D wext

